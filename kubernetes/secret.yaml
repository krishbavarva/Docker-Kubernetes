apiVersion: v1
kind: Secret
metadata:
  name: app-secrets
  namespace: fullstack-app
type: Opaque
stringData:
  SECRET_KEY: "secret-key-dev"  # In production, use: kubectl create secret generic app-secrets --from-literal=SECRET_KEY=your-secret-key
  # To create secret from command line:
  # kubectl create secret generic app-secrets --from-literal=SECRET_KEY=your-actual-secret-key -n fullstack-app


